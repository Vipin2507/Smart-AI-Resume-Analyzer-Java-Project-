# Copy to .env.local for local development
# For production build: use .env.production or set in Render

# Local dev (Vite proxy to backend)
VITE_API_BASE_URL=

# When empty, frontend uses relative /api (proxied by Vite to backend)
